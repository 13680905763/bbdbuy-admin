# 1. 依赖包 (由 pnpm install 自动生成，禁止上传)
node_modules/

# 2. 编译产物 (由 max build 生成的生产环境代码)
/dist/
*.zip

# 3. Umi 4 框架临时目录 (开发与打包时的缓存，由 max setup 生成)
.umi/
.umi-production/

# 4. 编辑器相关 (防止个人 VS Code 配置污染仓库)
.vscode/
.history/

# 5. 日志文件 (仅记录本地运行错误)
npm-debug.log*